version: "3.9"
services:
  api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ../..:/app
    command: bash -lc "pip install -r requirements.txt && uvicorn services.api.main:app --host 0.0.0.0 --port 8080 --reload"
    ports:
      - "8080:8080"
